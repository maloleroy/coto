[project]
name = "analysis"
version = "0.1.0"
description = "Performance analysis of additive quantum decision diagrams"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.14.1",
]

[tool.ruff]
exclude = ["data"]
line-length = 100
